from views.gui_verificating import gui_on


gui_on()

# parsed = parse(digital_signed_pdf)
# original_pdf = parsed[0]
# signature = parsed[1]
# hash = decrypt(signature)
# original_hash = hash(original_pdf)
# if hash == original_hash:
#   print(verified)
